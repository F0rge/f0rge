# B5 bank rules — Superdesign outcome

- **Canvas used:** no.
- **Credits:** `insufficient_credits` this session (billing gate). Did not call the Superdesign CLI.
- **Implementation:** IBM Carbon on `/bank-reconciliation`. Rules Modal (pattern + target, delete). Unmatched **Apply rule** is one confirm Modal. Recode journal-matched lines via account Select. No extra nav. No `@f0rge/ui`.
