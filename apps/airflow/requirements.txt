# Follow-up image layer for consumer DAGs (meal classification, etc.).
# Keep versions pinned; bump deliberately with the Airflow base image.
apache-airflow-providers-common-ai[openai]==0.7.0
apache-airflow-providers-amazon[s3fs]>=9.0.0
apache-airflow-providers-git>=0.0.4
