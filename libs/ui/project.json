{
  "name": "f0rge-ui",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/ui/src",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsc --noEmit -p libs/ui/tsconfig.json"
      }
    },
    "storybook": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run storybook --workspace=@f0rge/ui"
      }
    },
    "storybook-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run storybook-build --workspace=@f0rge/ui"
      }
    }
  },
  "//": "lint inferred by @nx/eslint from eslint.config.mjs. No build: source-consumed via transpilePackages.",
  "tags": ["platform:ts", "scope:shared"]
}
